# Canonical Terrain Mode

Status: ready-for-agent
Labels: enhancement, ready-for-agent

## Parent

`.scratch/d435-depth-camera-navigation/PRD.md`

## What to build

Add a `terrain` mode to the canonical hierarchical navigation entry point. The mode should preserve the existing terrain/trimesh obstacle demo behavior, using static terrain obstacles synced into navigation observations without feeding camera-derived obstacle slots to the navigation policy.

This slice gives the unified entry point a fixed-obstacle baseline that later depth debug and depth-camera modes can compare against.

## Acceptance criteria

- [x] The canonical entry point supports a `terrain` mode.
- [x] The mode configures terrain/trimesh obstacles consistently with the previous terrain demo.
- [x] The mode uses oracle obstacle slots from the environment, not D435 camera-derived slots.
- [x] The mode loads and uses the existing trained policies.
- [x] The mode prints terrain/navigation status comparable to the previous terrain demo.
- [x] The baseline terrain behavior remains available before old scripts are archived.

## Blocked by

- `.scratch/d435-depth-camera-navigation/issues/01-canonical-oracle-mode.md`

## Triage Brief

> *This was generated by AI during triage.*

Category recommendation: `enhancement`.

State recommendation: `ready-for-agent`.

This issue is ready. It depends on the canonical entry point shape from issue 01 and should not introduce D435 camera policy input.
