# Config-Driven D435 Camera Model

Status: ready-for-agent
Labels: enhancement, ready-for-agent

## Parent

`.scratch/d435-depth-camera-navigation/PRD.md`

## What to build

Move the simulated Intel RealSense D435 stream parameters and camera mount parameters into the hierarchical navigation configuration. The play/demo code should read D435 resolution, FOV, depth range, mount body, local position, roll, pitch, yaw, scan extraction settings, ground filtering settings, and obstacle clustering settings from config rather than script-level constants.

This slice makes camera placement and D435 stream assumptions explicit and reproducible before physical installation.

## Acceptance criteria

- [x] Hierarchical navigation config contains a D435/depth-camera section.
- [x] The config includes mount body, local position, roll, pitch, and yaw.
- [x] The config includes D435 width, height, horizontal FOV, vertical FOV, min depth, and max depth.
- [x] The config includes scan extraction parameters.
- [x] The config includes ground filtering and obstacle clustering parameters.
- [x] Existing depth scan and depth camera paths read camera settings from config.
- [x] Script-level D435 constants are removed or reduced to compatibility defaults.

## Blocked by

None - can start immediately.

## Triage Brief

> *This was generated by AI during triage.*

Category recommendation: `enhancement`.

State recommendation: `ready-for-agent`.

This issue is ready and mostly configuration/interface work. The agent should keep behavior-compatible defaults while making future camera mount tuning config-driven.
