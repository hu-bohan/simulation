# Canonical Oracle Mode

Status: ready-for-agent
Labels: enhancement, ready-for-agent

## Parent

`.scratch/d435-depth-camera-navigation/PRD.md`

## What to build

Create the first working slice of the canonical hierarchical navigation entry point by supporting an `oracle` mode. This mode should preserve the existing baseline behavior: load the trained low-level locomotion policy, recovery policy, and upper-level navigation policy; use environment-provided oracle obstacle slots; apply navigation actions to low-level commands; and run the existing hierarchical navigation demo path without relying on D435 camera input.

This slice establishes the new entry point without changing the depth-camera adapter or the policy interfaces.

## Acceptance criteria

- [x] There is a documented canonical entry point for hierarchical navigation demos.
- [x] The entry point supports an `oracle` mode that uses oracle obstacle slots, not camera-derived obstacle slots.
- [x] The mode loads and uses the existing locomotion, recovery, and navigation policies.
- [x] The mode keeps the existing low-level policy bridge behavior intact.
- [x] The mode prints navigation status comparable to the previous baseline demo.
- [x] The old baseline behavior remains recoverable until legacy archival happens.

## Blocked by

None - can start immediately.

## Triage Brief

> *This was generated by AI during triage.*

Category recommendation: `enhancement`.

State recommendation: `ready-for-agent`.

This issue is narrow and independently implementable. The agent should avoid D435 camera code and focus on preserving the existing oracle baseline through the new canonical entry point.
