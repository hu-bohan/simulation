# Offline Depth Adapter Contract

Status: ready-for-agent
Labels: enhancement, ready-for-agent

## Parent

`.scratch/d435-depth-camera-navigation/PRD.md`

## What to build

Create a pure Python validation seam for the D435 depth adapter so that geometry and navigation-observation behavior can be checked on this machine without Isaac Gym. The seam should validate that known synthetic scan or depth-like inputs produce expected obstacle estimates and expected 24D navigation observation obstacle slots.

This slice should make the adapter contract explicit: input units, scan angle convention, body-frame sign convention, obstacle slot ordering, missing-obstacle behavior, clearance calculation, radius handling, and normalization.

## Acceptance criteria

- [x] There is a pure Python way to validate the scanner/translator utilities without importing Isaac Gym.
- [x] Synthetic input can verify obstacle slot ordering for the four camera-derived obstacle slots.
- [x] Synthetic input can verify body-frame left/right sign convention.
- [x] Synthetic input can verify clearance and radius normalization.
- [x] Missing or far obstacles produce deterministic fallback slots.
- [x] The adapter contract is documented close to the validation seam.
- [x] The validation can run with `/home/hubohan/prp/easy_simulation/venv/bin/python`.

## Blocked by

None - can start immediately.

## Triage Brief

> *This was generated by AI during triage.*

Category recommendation: `enhancement`.

State recommendation: `ready-for-agent`.

This issue is ready because it has a clear non-Isaac test seam and does not depend on script consolidation. The agent should avoid changing policy architecture and keep the output aligned with the existing 24D navigation observation.
